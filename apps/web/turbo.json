{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "env": ["DEV_MODE", "SSR_GATEWAY_DOMAIN", "SSR_GATEWAY_API_KEY", "NEXT_PUBLIC_BASE_URL"]
    }
  }
}
